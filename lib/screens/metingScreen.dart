import 'package:flutter/widgets.dart';

class Metingscreen extends StatefulWidget {
  const Metingscreen({super.key});

  @override
  State<Metingscreen> createState() => _MetingscreenState();
}

class _MetingscreenState extends State<Metingscreen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}